import {theme} from "antd";

export default {
    token: {
        // colorPrimary: '#30b169'
        colorPrimary: '#29b240',
        // borderRadius: 10
    },
    components: {
        Button: {
            // borderRadius: 15
        }
    },
    algorithm: theme.defaultAlgorithm,
    // algorithm: theme.darkAlgorithm,
}